version: 1
metadata:
  name: 默认提供程序授权流程（隐式同意）
entries:
- attrs:
    designation: authorization
    name: 授权应用程序
    title: 正在重定向到 %(app)s
    authentication: require_authenticated
  identifiers:
    slug: default-provider-authorization-implicit-consent
  model: authentik_flows.flow
